<% content_for :title, "New Booking" %>
<% content_for(:page_title, "New Booking") %>

<%= render "form", booking: @booking %>

<div class="btn-group mt-2">
  <%= link_to "Return", venues_path, class: "btn btn-secondary btn-sm" %>
</div>

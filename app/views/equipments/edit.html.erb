<% content_for(:page_title, "Edit Equipment") %>

<div class="card" style="max-width:600px;">
  <div class="card-body">
    <%= render "form", equipment: @equipment %>
  </div>
  <div class="card-footer">
    <div class="btn-group">
      <%= link_to "Back to Equipment", equipments_path, class: "btn btn-secondary btn-sm" %>
    </div>
  </div>
</div>
